# --- Web / DB ---
Flask>=3.0,<4
Flask-SQLAlchemy>=3.1,<4
Flask-Migrate>=4.0,<5
SQLAlchemy>=2.0,<3
psycopg[binary]>=3.1,<4
python-dotenv>=1.0
itsdangerous>=2.1
cryptography>=42.0
requests>=2.31
gunicorn>=22.0

# --- Agent core ---
langchain-core>=0.3.36
langgraph>=0.2.60
langchain-mcp-adapters>=0.1.0
mcp>=1.9

# --- LLM providers (default: openai) ---
# The LLM is pluggable: set LLM_PROVIDER + LLM_MODEL in .env and install the
# matching package below. Only the one you use needs to be installed.
langchain-openai>=0.2
# langchain-anthropic>=0.3
# langchain-google-genai>=2.0
# langchain-ollama>=0.2
# langchain-groq>=0.2

# --- Dev ---
pytest>=8.0
